# Default values for oracleords.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

mongodb:
  version: "6.0.5"
  adminName: my-user
  adminPassword: testing
  mongotestName: "mongo_test"
  mongotestPassword: "MyPassword1!"

