apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oracleords.fullname" . }}-test-connection"
  labels:
    {{- include "oracleords.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oracleords.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
